---
title: "orch → B1: persistence task"
type: bus-message
status: sent
updated: 2026-09-18
related: ["[[spec]]", "[[employee]]", "[[task-item]]", "[[index]]"]
from: orch
to: B1
seq: 1
---

# B1 Persistence: task

You are node **B1 (Persistence, opus)** in `.wiki/plan/graph.yaml`. Schema work is never routed down: you own the
DbContext, the Fluent configurations, every constraint, index and trigger, the seed, and the initial migration.
B2 (application service) and B3 (integration tests) run in parallel in their own worktrees and code against the
**spec's** contracts, not against your code. So every name in spec §10–§13 must come out exactly as written.

Write `[uncertain]` instead of guessing. Report "not found" instead of inventing a path or API.

## First

1. Load the `ponytail:ponytail` skill with the Skill tool (human instruction for development nodes). What the spec
   requires is required.
2. Read `AGENTS.md`, then `.wiki/domain/spec.md` §2, **§2a**, §9, **§10, §11 (including the trigger SQL and "How B1
   adds it"), §12, §13**, then `docs/adr/0003`, `0007`, `0009`. Read the domain code in `src/TaskManagement.Domain/`
   (it's merged and final; you must not change it).

## Files you create (only these)

- `src/TaskManagement.Infrastructure/TaskManagementDbContext.cs`
- `src/TaskManagement.Infrastructure/TaskManagementDbContextFactory.cs`
- `src/TaskManagement.Infrastructure/Configurations/EmployeeConfiguration.cs`
- `src/TaskManagement.Infrastructure/Configurations/TaskItemConfiguration.cs`
- `src/TaskManagement.Infrastructure/Migrations/<timestamp>_InitialCreate.cs` and `<timestamp>_InitialCreate.Designer.cs`,
  and `src/TaskManagement.Infrastructure/Migrations/TaskManagementDbContextModelSnapshot.cs`, all generated by
  `dotnet ef migrations add InitialCreate --project src/TaskManagement.Infrastructure`
- `.wiki/agents/bus/B1-to-orch-001.md`: your report.

## Steps

1. Write the DbContext, the factory and the two configurations exactly per spec §10–§13, with XML docs per §2a. Each
   configuration class's docs name every index/constraint it creates and why; `TaskItemConfiguration`'s docs say
   the BR3/BR4 trigger lives in the migration, not in the model.
2. Generate the migration. Read the generated `Up()` and **confirm or refute** these open spec items, quoting the
   generated lines:
   - (a) the `status` column is `character varying(20)`;
   - (b) `InsertData` for `tasks` writes status as the strings `"New"`, `"Completed"`, `"Cancelled"`, not integers;
   - (c) `employees` rows are inserted before `tasks` rows;
   - (d) no extra default-named index such as `IX_tasks_assignee_id` exists; the migration has exactly the indexes
     and constraints of §11.
   If one fails, fix the **configuration** (never hand-edit the generated schema operations) and regenerate. Report
   what you changed.
3. Edit only `InitialCreate.cs`: add a header comment at the top listing every schema change (tables, columns, PKs,
   FKs with delete behaviour, indexes, CHECKs, the trigger function and trigger, seed rows). Add the trigger SQL of
   §11 with `migrationBuilder.Sql(...)` at the **end** of `Up()`, and the two drops at the **start** of `Down()`.
4. Prove it against a real database: start a throwaway container
   (`docker run -d --rm -e POSTGRES_PASSWORD=pw -p <free port>:5432 postgres:17-alpine`), run
   `dotnet ef database update --project src/TaskManagement.Infrastructure --connection "Host=localhost;Port=<port>;Database=task_management;Username=postgres;Password=pw"`,
   then capture `psql` `\d employees`, `\d tasks`, `SELECT * FROM employees`, `SELECT id, status, completed_at FROM tasks`
   and `SELECT tgname FROM pg_trigger WHERE tgrelid = 'tasks'::regclass AND NOT tgisinternal`. Also run one
   violating insert for BR3 (assignee Carol) and one BR4 update, and capture the errors. Then run `dotnet ef database update 0`
   with the same connection to prove `Down()` works (capture `\dt` and `\df tasks_enforce_br3_br4` after it), and
   remove the container.

## Do not touch

Everything else: `src/TaskManagement.Domain/**` (no attributes, no edits), `src/TaskManagement.Application/**`,
`tests/**`, every `.csproj`, `Directory.Build.props`, `.editorconfig`, and all docs outside your report. No new
packages.

## Acceptance (run it yourself, from the worktree root; all must exit 0)

```bash
dotnet build -warnaserror
dotnet ef migrations list --project src/TaskManagement.Infrastructure --no-connect
grep -q "CREATE TRIGGER trg_tasks_br3_br4" src/TaskManagement.Infrastructure/Migrations/*_InitialCreate.cs
grep -q "DROP FUNCTION IF EXISTS tasks_enforce_br3_br4" src/TaskManagement.Infrastructure/Migrations/*_InitialCreate.cs
! grep -rnE '\[(Key|Required|Table|Column|MaxLength|StringLength|ForeignKey|Index|NotMapped|DatabaseGenerated|ConcurrencyCheck|Timestamp)\b' src/TaskManagement.Domain --include=*.cs
```

Commit on your worktree branch: `feat(persistence): DbContext, Fluent configurations and initial migration`, with the
trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

## Questions

Send them to orch with SendMessage (`to: "main"`) and also write them to `.wiki/agents/bus/B1-to-orch-<seq>.md`. Keep
working on the rest.

## Report (`.wiki/agents/bus/B1-to-orch-001.md`, committed with your code)

Frontmatter like this file's (`from: B1`, `to: orch`). Then: status; files changed; the public API (DbContext,
factory, configurations); results (a)–(d) with the quoted generated lines; the full header comment; every command you
ran with its real output (build tail, migrations list, the `\d` outputs, the violation errors, the `Down()` check);
the worktree branch name and commit SHA; `[uncertain]` items; open questions.
